info:
  name: Get Note
  type: http
  seq: 3

http:
  method: GET
  url: "{{BACKEND_URL}}/notes/"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{X-CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
