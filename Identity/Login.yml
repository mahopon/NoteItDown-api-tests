info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{BACKEND_URL}}/user/login"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{X-CSRF-TOKEN}}"
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "P@ssw0rd123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
