info:
  name: Check Status
  type: http
  seq: 1

http:
  method: GET
  url: "{{BACKEND_URL}}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
